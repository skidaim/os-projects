# OS hmwk3: Expose Page Table
## Expose the page table of POSIX processes’ running on Linux X86_64 and arm64 into user-space, in a flat table format.
### Original assignment statement and submission files @ project-files folder
